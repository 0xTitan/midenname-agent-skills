.DS_Store

# Video / screen recordings
*.mov
*.mp4

# Generated by scripts/miden-name.sh: Cargo.toml is rendered from the template
# (machine-specific path dep); Cargo.lock is seeded from the contracts clone's lock
# to pin the shared miden crates. Both are runtime-generated, not committed.
tools/miden-name/Cargo.toml
tools/miden-name/Cargo.lock

# Rust build artifacts
target/
tools/**/target/
